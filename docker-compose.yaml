version: '3'

services:
  gateway-service:
    build:
      context: .
      target: development
    ports:
      - 9000:9000
    environment:
      STAGE: local
    volumes:
      - .:/home/node
    command: /bin/sh -c "npm i && npm run start:dev"
    networks:
      - bng

networks:
  bng:
    external: true
